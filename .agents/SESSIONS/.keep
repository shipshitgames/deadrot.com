# Keeps this directory in git. Session logs here are gitignored on purpose
# (see .gitignore: `.agents/SESSIONS/*` + `!.agents/SESSIONS/.keep`) — they are
# auto-generated and must not be committed to this public repo.
