node_modules/
.turbo/
.artifacts/
*.tsbuildinfo
next-env.d.ts
.next/
dist/
out/
playwright-report/
test-results/
coverage/
tmp/
.vercel/
*.log
.DS_Store
.env
.env.*
!.env.example

# git worktrees created by the /worktree skill
.worktrees/
.claude/
.claude/worktrees/

# Git LFS hooks generated by local checkout setup; pre-commit is tracked.
.githooks/post-checkout
.githooks/post-commit
.githooks/post-merge
.githooks/pre-push
.vercel

# Agent session logs: keep the dir (.keep) but never commit its contents — this is
# a PUBLIC repo and auto-generated session summaries don't belong in git.
.agents/SESSIONS/*
!.agents/SESSIONS/.keep

# Local credentials and private keys
.aws/
**/.aws/
*.pem
*.p12
*.pfx
*.private-key
*.key
*.ppk
id_rsa*
id_ed25519*
.pgpass
*.pgpass

# Secret-bearing artifacts (incident hardening 2026-06-18). Patterns are scoped
# to credential-shaped filenames so they never swallow legit files like
# .github/workflows/secret-scan.yml or scripts/check-secrets.mjs. Real secrets
# belong in AWS SSM Parameter Store, never on disk.
*.secret
*.secrets
secrets.json
secrets.yaml
secrets.yml
*password*.txt
*passwd*.txt
*-url-*.txt
*-url.txt
*-db-url*.txt
*.connection-string
*credentials*.json

# Local source archives generated while preparing runtime assets. Curated,
# successful generation history under sources/generated is intentionally tracked.
packages/assets/sources/*
!packages/assets/sources/generated/
!packages/assets/sources/generated/**
