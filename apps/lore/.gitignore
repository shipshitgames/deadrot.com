.DS_Store
node_modules
public
prof
tsconfig.tsbuildinfo
.quartz-cache
private/
.replit
replit.nix

# package-lock not used — the monorepo is bun (root bun.lock)
package-lock.json

# Lore must not own binary art/media. Move assets to packages/assets and link them
# from notes instead of hiding vault-local copies here.

# OS noise inside the vault
content/**/.DS_Store
.vercel
