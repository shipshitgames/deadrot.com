{
  "name": "deadrotcom",
  "version": "0.0.0",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "apps/games/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "lint:packages": "turbo run lint",
    "e2e": "turbo run test:e2e",
    "test:e2e": "playwright test",
    "e2e:docker": "scripts/run-e2e-docker.sh",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report",
    "assets:check": "node scripts/check-assets.mjs",
    "board:hygiene": "node scripts/board-hygiene.mjs",
    "deploy:games:changed": "node scripts/deploy-changed-games.mjs",
    "typecheck": "turbo run typecheck",
    "ci": "bun run format:check && bun run lint && bun run typecheck && bun run assets:check",
    "deps:update": "bunx npm-check-updates -u --deep --cooldown 3h --removeRange && bun install",
    "doctor": "react-doctor",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "@playwright/test": "^1.60.0",
    "react-doctor": "^0.4.0",
    "turbo": "2.9.16",
    "typescript": "6.0.3"
  }
}
