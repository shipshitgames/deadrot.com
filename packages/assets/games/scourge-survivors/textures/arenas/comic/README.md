# Comic Arena Runtime Textures

First playable comic-style arena texture exploration for Scourge Survivors.

The catalog overrides only the generic `arena-floor`, `arena-wall`, `arena-block`, and `arena-column` ids when `VITE_DEADROT_COMIC_ASSETS=1`.
