# Shared Audio

Game-agnostic SFX and ambience shared across the Scourge universe.

First-pass placeholder slots:

- `breach-collapse.ogg` — a breach (Scourge nest) collapsing once its repeater
  is severed.
- `choir-whisper.ogg` — the hive-mind ("the Choir") ambient whisper bed.
- `hellfire-pulse.ogg` — short Pyre/hellfire pulse cue.

These are referenced from `assets-catalog.json` under `shared` (`kind:
"audio"`). Replace the synthetic placeholders with final studio-licensed cues
when the audio pass lands.
